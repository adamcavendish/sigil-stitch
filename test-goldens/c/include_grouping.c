#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "config.h"
#include "utils.h"

int x = printf("hello");
void* p = malloc(sizeof(int));
size_t n = strlen("test");
Config cfg;
helper();
